node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage

# Source animation frames + scratch (large; the built webp is what we ship)
frames_8fps/
.tmp-frames/

# Raw FLUX art stays out of the repo (large). We ship the optimized WebP
# (run `npm run optimize` after `npm run art`), which IS committed and served.
public/art/*.png
public/art/*.jpg

# Generated SFX, music and the small video clips ARE committed so the deployed
# app has them. Regenerate with `npm run sfx` / `npm run video`.
